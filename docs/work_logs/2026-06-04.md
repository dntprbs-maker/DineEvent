# 2026-06-04 작업 일지

## 작업 내역 요약

1. **Firestore 보안 규칙 적용**
   - `firestore.rules` 파일 생성 및 기본 접근 제어 룰 반영

2. **코드 린트 오류 대폭 수정 및 최적화**
   - `useEffect` 내 비동기 함수 순서 이동에 따른 런타임 호이스팅 에러 및 `exhaustive-deps` 문제 완화
   - 프로젝트 전체에 걸친 불필요한 `import React` 구문 및 미사용 컴포넌트, 변수(`db`, `idx`, `activeFaq`, `navigate` 등) 제거
   - `eslint.config.js` 수정하여 데이터 패칭 패턴에서 발생하는 `set-state-in-effect` 경고 수준 완화

3. **손상된 파일 파싱 에러 복구**
   - 인코딩/스크립트 문제로 파싱 오류가 나던 `NewLanding.jsx`와 `OriginalIntro.jsx`를 백업 파일로부터 성공적으로 복구 후 재수정

4. **미사용 코드의 UI 활성화 (기능 고도화)**
   - 미사용 상태이던 `totalEntries` 상태 변수를 `CompanyIntro` 및 `CompanyIntroV2`의 히어로 섹션 배지 UI에 연결하여 라이브 카운팅 시각 효과 부여
   - 사용되지 않던 `startVirtualSpin` 함수를 CTA 버튼 클릭 이벤트에 연결하여 가상 룰렛 체험 기능 활성화 및 `eslint-disable` 주석 불필요 요소 제거
